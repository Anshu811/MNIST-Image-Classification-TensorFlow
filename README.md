# MNIST-Image-Classification-TensorFlow
The main objective of this project is to design, train, and evaluate a neural network model that can correctly classify grayscale images of handwritten digits (0–9) using the MNIST dataset.
